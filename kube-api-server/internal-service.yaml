apiVersion: v1
kind: Service
metadata:
  name: internal-api-service-1
spec:
  selector:
    app: internal-api-service-1
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5001
  type: ClusterIP
